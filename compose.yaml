services:
  api:
    build: ./services/api
    env_file:
      - ./services/api/.env
    environment:
      - DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - REDIS_URL=${REDIS_URL}
      - WEBSITE_URL=${WEBSITE_URL:-https://flowlog.ru/}
      - QUICK_CALC_URL=${QUICK_CALC_URL:-https://t.me/AiFlowlog_bot}
      - LEAD_SOURCE=${LEAD_SOURCE:-transrussia_qr}
      - API_SECRET=${API_SECRET}
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    depends_on:
      - postgres
      - redis
    restart: unless-stopped


  bot:
    build: ./services/bot
    env_file:
      - ./services/bot/.env
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - API_BASE_URL=http://api:8000
      - BOT_PUBLIC_URL=${BOT_PUBLIC_URL}
      - BOT_SECRET=${BOT_SECRET}
      - API_SECRET=${API_SECRET}
      - MANAGER_GROUP_ID=${MANAGER_GROUP_ID}
      - REDIS_URL=${REDIS_URL}
      - WEBSITE_URL=${WEBSITE_URL:-https://flowlog.ru/}
      - QUICK_CALC_URL=${QUICK_CALC_URL:-https://t.me/AiFlowlog_bot}
      - LEAD_SOURCE=${LEAD_SOURCE:-transrussia_qr}
    depends_on:
      - api
      - redis
    restart: unless-stopped

  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_HOST_PORT:-55433}:5432"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redisdata:/data
    ports:
      - "${REDIS_HOST_PORT:-6380}:6379"
    restart: unless-stopped

  nginx:
    image: nginx:1.25-alpine
    depends_on:
      - api
    ports:
      - "${NGINX_HOST_PORT:-8081}:80"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro


volumes:
  pgdata:
  redisdata:
  certs:
